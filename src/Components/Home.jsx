import PhotosFeedModal from './PhotosFeed/PhotosFeedModal';

const Home = () => {
  return (
    <section className="container mainContainer">
      <PhotosFeedModal />
    </section>
  );
};

export default Home;
