import styles from './Account.module.css';

const AccountNewPhoto = () => {
  return (
    <section className={`${styles.account} container`}>
      <h1 className="title">New Photo</h1>
    </section>
  );
};

export default AccountNewPhoto;
